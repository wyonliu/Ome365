---
title: Ome365 v1.2 企业级AI平台设计 · 中文最终版
version: r5.0-final-zh (10轮多专家迭代后的中文重写 · 不是机翻 · 重新组织过)
date: 2026-05-13
status: ship-candidate
audience: 客户CHRO / 合规总监 / CFO / CIO / CTO / DPO + 内部团队
related:
  - docs/strategy/v1.2-enterprise-ai-platform-design.md（英文同步版）
  - docs/strategy/v1.1-team-brain-design.md
  - docs/policies/EVAL_USAGE_POLICY.md
---

# Ome365 v1.2 企业级AI平台设计（终版）

## 〇 · 一句话定位

**先卖可审计的、文件优先的治理底座（护城河），让cockpit成为日活产品（粘性）。**

v1把cockpit当主角，把治理当配件——结果10位评审专家5位拒签。
v4把这条主从关系倒过来。治理底座是规模化、可被监管认可的，
cockpit是底座之上才能跑起来的可视化。

---

## 〇.1 · 买家与楔子

**目标买家**（有名有姓）：**500-2000人规模的受监管行业企业的
CHRO + 合规总监/总法律顾问**。行业典型：金融科技、医疗科技、
合规科技、受监管制药。痛点一句话：**Lattice/Glean跑不通2027年
EU AI Act + PIPL §38的审计**。

**核心楔子**（一句话）：

> **Workday和Lattice是HR的record system；它们不是"动了HR的AI"的
> 可审计record system。Ome365是。**

**对四个竞品类目的分场景楔子**：

- **对Workday/Lattice**："它们是HR的系统记录；我们是动了HR的AI的可审计记录。"
- **对Glean/Notion**："它们搜索和总结你的数据；我们能证明某主体行使Art 17后，所有衍生embedding和摘要发生了什么。"
- **对Mem0/Letta/Zep**："它们是先存memory后补治理的SDK；我们是先有治理再原生存memory的底座。"
- **对Vanta/Drata/OneTrust/Credo**："它们告诉你哪些系统存在、哪些策略写好了；我们记录每条prompt对这些系统做了什么，且可密码学校验。"

---

## 〇.2 · 八条差异化（可验证的硬指标）

第6轮跨产品评估覆盖4个产品类目 × 11款命名产品（Workday、Lattice、
Glean、Notion AI、Mem0、Letta、Zep、Cognee、Vanta、Drata、OneTrust +
Credo/Cranium）。**这11款里，没有一款同时具备以下8条中的4条。**

| # | 差异化（可测试的命题） | 当前最接近者 |
|---|---|---|
| 1 | **HSM/KMS加持的密码学擦除权**：逐主体envelope密钥销毁；Art 17 / PIPL §47靠"key-ID销毁证据"验证，而不是"数据库行删除"凭信任 | 无——Lattice/Glean/Notion都是"按策略删除" |
| 2 | **逐prompt的Sigstore Rekor + RFC 3161双锚**：每条prompt/Decision钉到公开透明日志 + RFC-3161时间戳过的Merkle根；每日发射完整性计数器对账 | 无——Vanta的日志在租户内，Workday的journal也在租户内 |
| 3 | **区域可证明的LLM路由+硬阻断**：provider attest执行区域（AWS STS / Anthropic响应头 / Azure managed identity）；未attest则PIPL §38或GDPR Chapter V租户的出站LLM**直接阻断**，不是警告 | 无——Glean有单租户区域，但谁也不证明LLM调用本身 |
| 4 | **同平台共存WORM + 密码学擦除**：object-lock S3 / MinIO retention满足FINRA 17a-4(f) / SEC 17a-4 / MiFID II，**同一份记录**通过key销毁支持GDPR Art 17。互斥的两套监管在同一底座并行通过 | 无——Vanta有SOC2没WORM，QLDB有WORM没shred |
| 5 | **三角色独立审计签字**对raw-prompt访问（tenant-admin + DPO + auditor三位真人；<500人租户需外部attestation） | 无——其他厂商基本都是单角色admin |
| 6 | **DID签字的个人memory + 经理证据注入**：ed25519 manager-DID签字；anti-self-injection规则（manager-DID ≠ subject-DID）封住SOX 404 material weakness缺口 | 无——Lattice review评论无签字；Mem0/Letta无DID |
| 7 | **文件优先的Markdown vault + Apache 2.0 + Ollama默认**：record source是可移植的明文+frontmatter；Vanta/Glean/Lattice/Notion全是SaaS租户锁定 | LangMem（SDK）、Cognee（控制面）勉强同方向，但都没治理 |
| 8 | **绕开租户DLP的吹哨人OWA通道**：Dodd-Frank §922 + EU Whistleblower Directive合规；导出走外部公证人，不出现在租户admin的出站日志里 | 无——Lattice靠第三方Vault，其他都没内建 |

---

## 〇.3 · 与11款竞品的并表比较（第6轮 · 4个分析agent报告综合）

### 〇.3.1 vs Workday Performance + Workday AI (2026)

- **定位**：T1级HCM大套件，性能模块捆绑Talent/Comp/Recruiting。
  AI层：Illuminate agent家族（Recruiter/Manager Insights/Career/
  Succession）、Vibe分析、生成式review摘要、skills-cloud推理。
- **定价**：$100-300/人/月全包，Illuminate add-on约$10-15/人/月。
- **审计**：SOC 2 Type II + ISO 27001 + FedRAMP Moderate。**audit
  journal在租户内**，无公开transparency log。
- **典型客户**：Fortune 2000 / 3000人以上 / 重IT团队。
- **对Ome365目标买家的弱点**：9-18个月实施周期、首年100-300万美金、
  AI agent是黑箱（无逐决策证据链）、数据在Workday自己的租户、
  GDPR Art 17的擦除是"提删除工单"，不是key destroy。

### 〇.3.2 vs Lattice (2026)

- **定位**：中端市场"people success"平台，5000+客户，UX极强。
- **定价**：$11/人/月Talent + $4 AI add-on，捆绑约$15/人/月。
- **审计**：SOC 2 + GDPR DPA；**无FINRA 17a-4 WORM、无Rekor式
  transparency log**。
- **客户**：200-2000人科技/服务业的CHRO群体。
- **弱点**：SaaS-only（无self-host）、无HSM-backed密码学擦除、
  吹哨人模块用第三方Vault、AI prompt和输出**不可密码学attest**。

### 〇.3.3 vs Glean (2026)

- **定位**："Work AI"——企业搜索+Glean Assistants，40+ connector。
- **定价**：base约$50/人/月 + AI add-on约$15/人/月；约100人起步，
  ACV底线约6万美金，大单20-48万美金全包。
- **审计**：permission-aware RAG、SOC2、单租户部署。
- **GDPR擦除路径**：依赖**re-crawl原数据源**——下次同步时索引条目
  evict，但**对chat记录、agent memory、摘要文件的传递性派生擦除
  不发布保证**。
- **对Ome365目标买家弱点**：定价不透明、无密码学审计、agent行为
  日志描述不完整、强制起跳定价让中端受监管客户付溢价、source of
  truth留在第三方SaaS（无markdown egress）。

### 〇.3.4 vs Notion AI / Q&A (2026)

- **定位**：协同工作区+原生AI Q&A。
- **审计**：Enterprise audit log（CSV导出）、SOC2 Type II / ISO
  27001/27701；**AI specific的action approval还是个开放问题**。
- **数据驻留**：欧盟（法兰克福）、美国、2026-05新增日本/韩国——
  仅Enterprise tier。
- **弱点**：工作区就是source of truth（无中立markdown vault）、
  无逐主体密码学擦除、无防篡改审计、AI运行日志有限。

### 〇.3.5 vs Mem0 (2026)

- **背书**：Y Combinator，2025-10 Series A 2400万美金（Basis Set /
  Peak XV / GitHub Fund / YC）。
- **数据**：约41k GitHub star，1400万PyPI下载，8万云上注册；AWS
  把Mem0定为Agent SDK的独家memory provider。
- **License**：Apache 2.0 OSS + 托管云。
- **治理**：SOC 2、GDPR posture、audit log、企业tier有HIPAA/BYOK。
  擦除是delete-by-API，**无密码学销毁证明、无传递性派生擦除**。

### 〇.3.6 vs Letta（前MemGPT，2026）

- **背书**：UC Berkeley Sky/BAIR分拆，Felicis领投1000万美金。
- **数据**：主仓约6.3k star + letta-code 2.4k。
- **定位**："stateful agent"——agent runtime里memory是LLM用tool
  call编辑的一等公民block。
- **治理**：基本只有标准云控制；无audit chain、无DID。

### 〇.3.7 vs Zep / Cognee

- **Zep**：商业版，Graphiti是开源核（temporal-graph）。声称在
  LongMemEval上63.8% vs Mem0 49.0%（GPT-4o）。Credit-based定价。
  治理是标准SaaS，无DID/HMAC chain。
- **Cognee**：MIT license，约6000+ star，约70个客户logo。纯OSS
  memory控制面、embedding + graph、GitHub Secure OSS毕业生。
  **目前还没有企业治理界面**——只过了安全review。

### 〇.3.8 vs Vanta + Drata (2026)

- **数据**：Vanta约15000客户、2.2亿美金ARR、AI Agent 2.0于2026-01
  推出；Drata约8000客户、ARR1亿美金+。
- **定位**：**系统级**SOC2/ISO27001自动化合规duopoly。证据模型 =
  集成只读拉取（Vanta约375、Drata约270）配置快照然后映射到控制库。
- **对LLM审计买家的弱点**：审计的是"员工配置了哪些系统"，
  **不是"prompt看到了什么、触发了什么tool call、模型emit了什么"**。
  无Rekor锚定、无HMAC chain、无发射完整性计数器、无逐record
  密码学擦除。

### 〇.3.9 vs OneTrust + Credo AI / Cranium

- **定位**：AI治理 **策略和资产清单**层——AI use case intake、
  model registry、policy pack、RoPA/DPIA、生命周期检查点、agent
  discovery。Cranium是AI安全姿态 + 三方AI合规attestation。
- **弱点**：回答"你为这个模型写了策略吗"，**不回答"prompt号
  4812719在03:14 UTC执行时策略被严格执行了吗、能否证明它没被改"**。
  是清单+策略撰写，不是**runtime防篡改ledger**。无Sigstore Rekor、
  无RFC 3161、无prompt级别三角色审计签字。

---

## 〇.4 · 四问杀器（Ome365能答、单一竞品答不全四条）

1. **能否密码学证明这个决策没被事后篡改，且无需信任HRIS厂商？**
2. **当主体行使Art 17时，能否给出"每一个embedding、摘要、引用片段
   都被定位、tombstone、待重蒸馏"的可审计trail？**
3. **能否回放agent发出的每一条prompt和tool call，由外部审计人员
   独立校验完整性？**
4. **能否拿出"LLM对你的系统做了什么"的证据——而不只是"哪些系统
   被正确配置了"？**

---

## 〇.5 · 诚实让步（v1.2不追赶的8条）

| # | 让步条目 | 持有者 | Ome365策略 |
|---|---|---|---|
| 1 | 40+预制ACL感知connector（Slack/Drive/SF/Confluence/Jira） | Glean | 不追赶；社区connector放到v1.4 |
| 2 | 600亿美金的HRIS深度（payroll/comp/recruiting/succession一整套） | Workday | 不追赶；做集成、不做替代 |
| 3 | calibration UX打磨 + 5000名经理adoption pattern | Lattice | v1.3做一轮cockpit打磨 |
| 4 | 41k star + 8万注册 + AWS Agent SDK独家partnership | Mem0 | 不同品类；用楔子区分（见§〇.1） |
| 5 | 可编辑memory block + Berkeley CS agent runtime血统 | Letta | 不同品类；引用而非竞争 |
| 6 | 400+预制控制映射 + 审计师关系 | Vanta | 做合作伙伴，不替代；trace映射成Vanta evidence |
| 7 | AI use case intake + model registry + 策略撰写 | Credo/OneTrust | 做合作伙伴；我们是runtime控制面、它们是策略库 |
| 8 | 网络效应UX + 协同编辑底盘 | Notion AI | 不追赶；支持markdown导出到Notion |

---

## 〇.6 · v3到v4变了什么（第7轮压力测试后的10条delta）

| # | v3 | v4 | 触发轮 |
|---|---|---|---|
| 1 | KMS根同时解DEK和HMAC | **HMAC种子隔到独立的Vault Transit角色**（split-trust，一天工作量） | R7 #1 KMS妥协 |
| 2 | manager-DID ed25519存储未指定 | **manager-DID默认HSM绑定**（YubiHSM或CloudHSM）；fallback到OS keyring需DPA披露 | R7 #1 |
| 3 | vault tar exfil无对策 | **W7交付TPM/Nitro-Enclave绑定的KMS grant**（或显式DPA披露 + v1.2.1热修路径）+ KMS CloudTrail进trace管线 + fs级read tripwire | R7 #3.2 vault tar exfil |
| 4 | LLM outage产生"无审计数据"歧义 | **`router.reject`行 + 每日Sigstore manifest里的`degraded_count`** + 可选Ollama自动fallback | R7 #3.1 |
| 5 | SCIM rename歧义 | **带版本的canonical DID** + `name_resolver`服务 + `scim.rename`锚定trace行 | R7 #3.3 |
| 6 | Sigstore身份 = 环境OIDC | **Workload绑定的Sigstore身份策略**：verifier拒绝非租户pin的Fulcio subject | R7 #1 |
| 7 | 单锚（Rekor）+ 单TSA | **双锚W5确认**：Rekor（Sigstore）+ RFC 3161 TSA；**Bitcoin OTS三锚排进v1.3** | R7 #2.6 |
| 8 | 预shred可能撞上subpoena | **监管watch list预shred闸门**：被覆盖类目（broker-dealer / 医疗 / 现役SOX/SEC reg-watch员工）的shred事件须3角色复议 | R7 #2.1 |
| 9 | 买家叙事散落 | **§〇.2八条差异化 + §〇.4四问杀器移到首页可见** | R6 + CEO Jay要求 |
| 10 | "Workday vs我们"未明确 | §〇.1分类目楔子 + §〇.3并表表格 | R6 |

---

## 一 · 角色画像（10位 · 三类）

> 核心原则：每个章节明确**谁把关、谁签字**。R4 critique log完整存档在
> `docs/strategy/v1.2-round-4-critique-log.md`。

### 1.1 买家（签合同的人）

| 角色 | 职位 | v1.2要给他/她的东西 |
|---|---|---|
| Carol | CHRO / 人力总负责 | calibration底座 + 受保护类别公平性审计 + BambooHR桥 |
| Henry | 合规总监 / GC | SOX/FINRA/SEC/EU AI Act映射 + WORM层 + 传票应对包 |
| Fiona | CFO | 审计包SKU + TCO模型 + 防作弊cost指标 |

### 1.2 用户（日活的人）

| 角色 | 职位 | v1.2要给他/她的东西 |
|---|---|---|
| Bob | 部门经理 | coaching gap图 + 晋升包导出 + 1:1准备 |
| Alice | 资深IC | 个人memory隔离 + 查询通知24h + 可移植签字记录 |
| Inko | 应届IC | onboarding模式 + AI-assist规范 + 安全提问通道 |

### 1.3 把关人（不爽就否决的人）

| 角色 | 职位 | 否决条件 |
|---|---|---|
| Dan | CIO / IT负责 | Sigstore审计、6层RBAC、采购形态 |
| Eve | DPO | 密码学擦除、传递性擦除原则、DPIA、PIPL §38 |
| Kris | 客户CTO | 运营韧性、8周诚实交付、研究问题诚实命名 |
| Jay | Ome365 CEO（内部meta角色） | 楔子清晰度、护城河、单ICP聚焦 |

---

## 二 · 七层架构（v4）

```
┌─────────────────────────────────────────────────────────────────┐
│  L7 商业层    审计包SKU · 三年TCO · 防作弊采样                     │（v3新增）
│  L6 界面层    Vue 3 cockpit · v1.2只交付经理cockpit                │
│  L5 API层     FastAPI · 12个v1.1模块 + 4个v1.2模块                 │
│  L4 派生层    eval() · trace() · wiki蒸馏 · region pinning         │
│  L3 状态变更  Decision · git commit · 双锚批量上链                  │（v4 双锚明确）
│  L2 vault层   markdown + 逐主体密文 + TPM绑定                       │（v4 TPM绑定）
│  L1 身份层    带版本DID + Skill记录 + HSM绑定签字                   │（v4 带版本 + HSM）
│  L0 密钥层    KMS DEK + Vault Transit HMAC（split-trust）           │（v4 split-trust）
└─────────────────────────────────────────────────────────────────┘
```

### 2.1 四条不可破坏的不变量

1. **L2才是真理**：vault是source of truth，cockpit只是view。
2. **L0必须HSM-backed**：v1.2前提条件，没有HSM的crypto-shred就是
   纸糊的——除非客户在DPA里明确接受残余风险。
3. **L4是合规咽喉**：所有跨境路由、purpose限制、retention判断都
   在L4闸门里出，不在API层。
4. **L7是客户承诺层**：SKU、SLA、防作弊控制、TCO承诺都在L7；
   没有L7就没有可签合同的产品。

---

## 三 · 四大旗舰功能（v1.2诚实交付）

### 3.1 员工维度可视化 · 6维 × 5档

**v1.2 W1-W3交付**：

- **6个维度**：交付 / 质量 / 战略清晰度 / 影响力 / 成长 / 协作。
- **5档公开 + 15级内部浮点**（浮点视图带审计行）。
- **角色族adapter**：`vault/Roles/<family>.md`定义证据schema。
- **假期感知归一化**：产假、医假、sabbatical不计入业绩归一化。
- **新人模式（onboarding mode）**：入职前180天展示"在学/在建/
  在爬坡/扎实/超前"，**不展示"不达标/发展中/达标/超出/卓越"**。
  在爬坡期内任何薪酬/晋升决策不得引用band。
- **浏览器里不展示并排标量**（反立场1）。
- **AI-assist gradient规范**：每个dim × band下推荐的AI使用浓度都
  写在`docs/policies/AI_ASSIST_NORMS.md`。

**v1.2不交付（推迟到v1.3-v1.4）**：

- HR侧可编辑calibration room（v1.2只读）
- 经理宽严尺overlay（v1.2只准备数据层）
- 受保护类别公平性审计仪表板（v1.2出数据层、v1.3做UI）
- 跨组织Skill VC验证（v1.4）

**诚实坦白**（来自Jay的楔子反思）：dim系统本身**不是护城河**。
Lattice和Workday半年就能复制。**vault + audit + crypto-shred底座**
才是。v1.2先把底座做对，dim cockpit骑在底座上。

### 3.2 AI记录 · 生产级防篡改trace

**Trace行schema**（v2基础 + 3个新字段）：

```jsonl
{
  "ts": "2026-05-13T...",
  "actor_did": "did:ome365:tenant1:alice",
  "decision_id": "2026-05-13-acme",
  "prompt_hmac": "...",
  "model_id": "claude-haiku-4-5",
  "tokens_in": 1024, "tokens_out": 487,
  "cost_usd": 0.034,
  "latency_ms": 1280,
  "outcome_id": null,
  "legal_basis": "art-6-1-f",
  "purpose": "decision-resolution",
  "retention_until": "2027-05-13",
  "recipient_country": "DE",
  "transfer_mechanism": "none",
  "execution_region_attest": "aws-...",
  "sigstore_anchor": "rekor:...",
  "rfc3161_token": "..."
}
```

**锚定策略**（Kris要求修订）：

- **内联**：HMAC + RoPA字段，同步、快路径。
- **每日批锚**：把过去24h的trace Merkle化，根锚到Sigstore Rekor +
  RFC 3161 TSA。异步、durable queue + DLQ + replay。
- **发射完整性attestation**：每日`LLM调用计数 vs trace行计数`签字
  对账，作为独立事件投到Rekor。审计师有RBAC可以验。

**Hash策略**：

- HMAC-SHA256（per-tenant 90天轮转密钥，redacted prompt）。
- 验证器走多代密钥校验历史行。
- **raw prompt存储**：**三角色独立审计签字**（tenant-admin + DPO +
  auditor三位不同的真人，写进`rbac-matrix.md`）。SoD不可能的<500人
  租户**必须用外部attestation**——不是"双签字"那么轻。
- **PIPL §38跨境**：full prompt存储**在结构上被阻断**（不是开关），
  直到租户部署China Standard Contract。

**区域attest**：

- `recipient_country` = provider attest的执行区域信号。AWS Bedrock
  靠STS、Anthropic靠响应头、Azure靠managed identity。Ollama本地：
  租户在部署时attest。
- provider不attest → 字段为`"unknown"` → PIPL §38 / GDPR Chapter V
  租户的出站LLM被**阻断**。自报区域不算合规依据。

**逐员工可见性**：单prompt行只对**audit RBAC**可见。经理界面看
团队级聚合（n ≥ 15）。"应届员工烧了4倍token"**不可作为经理查询**——
这是设计意图。

**运营故事**（Kris）：

- trace管线 = `LLM router → durable queue (RabbitMQ/Kafka) →
  trace-write worker → 每日Merkle batcher → Sigstore client`。
- Rekor outage：写继续进queue；batcher退避；4h backlog报警。审计
  gap window写进DPIA。
- TSA outage：切到备用TSA（可配置列表）；fallback成Rekor-only。
- HMAC key轮转：90天周期；启动时加载多代列表；老代保留7年用于
  历史验证。

### 3.3 知识库 · review队列 + 人工传递性擦除

**v1.2 W6交付**：

- **L1 Raw**：每个数据源带consent token。
- **L2 Event**：Hike v0.1（已交付）。
- **L3 Distilled**：review队列 + RBAC `wiki:approve`。
- **L4 Maintainer**：自动merge**仅限**低风险类目（typo、链接腐烂、
  redirect）；其他改动入人审队列。
- **应届贡献通道**：`wiki:propose` RBAC（在`wiki:approve`之下）；
  提案者merge时计入Growth维度。

**传递性擦除（v1.2人工workflow）**：

- L0 erasure事件 → 投`re-distill-ticket`到admin队列。
- 受影响的`vault/Knowledge/`条目flagged "redaction-pending"。
- 人审者（RBAC `wiki:approve`）决定：从剩余数据源re-distill / 整条
  purge / 标legal-hold阻断。
- 受影响的sqlite-vec embedding显式purge + 重建。
- 审计注：`redacted on YYYY-MM-DD per DSAR #N · reviewer:
  $auditor_did · remaining sources: N · cf. legal hold #M if blocked`。

**v1.2不交付（推迟到v1.4研究）**：

- **L3自动re-derivation**——开放研究问题。Pinecone / Weaviate /
  LangChain都没交付可比方案。
- **embedding残留断言**——"被擦除主体的cosine recall为零"是开放
  研究问题，无已发表技术。

**研究诚实**（Kris）：v1.4发表Ome365的embedding leak threat model +
reference detection。在此之前，v1.2客户拿到人工workflow + DPA里
明确的残余风险披露。

### 3.4 组织数据治理 · 8根杠杆

| # | 杠杆 | v1.2交付 | v1.3+ |
|---|---|---|---|
| 1 | 身份（DID） | 发行人中立签字 | 跨组织Skill VC v1.4 |
| 2 | RBAC | 6层 + 区域pin + auditor + DPO | 3角色SoD enforcement v1.3 |
| 3 | 防篡改审计 | Sigstore + RFC 3161每日批 + 发射完整性 | 流式锚 v1.4 |
| 4 | 密码学擦除retention | **HSM/KMS** envelope key + 35天备份轮转 | KMIP v1.3 |
| 5 | 传递性擦除 | 人工workflow + tombstone | 自动re-derivation v1.4 |
| 6 | 法医级grep | 查询通知24h + Rekor锚定legal-hold registry + 吹哨人通道 | 7年默认 / 可配置 v1.4 |
| 7 | HRIS桥 | **BambooHR入站only** + Workday/Personio CSV | Workday SCIM v1.3 |
| 8 | WORM层 | **新增**：object-lock S3 / MinIO retention；FINRA 17a-4(f)合规 | QLDB可选 v1.4 |

**L0侧的legal hold闸门**（Henry）：L0 keystore订阅legal-hold registry；
针对被hold命中的subject S的crypto-shred事件**被阻断**；阻断时返回
工单给GC处理。Hold registry本身锚到Sigstore。

**吹哨人bypass**（Henry）：OWA通道（`/api/owa/export`）接收签字
导出请求，绕开tenant-admin DLP签字流程，生成密封包发SEC / EU监管 /
集体诉讼counsel。审计trail走Sigstore + 独立的外部公证人（不在
租户控制内）。**吹哨人不出现在tenant-admin的DLP出站日志里**。

---

## 四 · v1.2交付范围（8周 · 2026-06至2026-07）

> 严格遵守**1 ICP / 1 cockpit / 1 HRIS / 1 region**。

| W | 交付物 | 把关角色 |
|---|---|---|
| W1 | 6维 + 5档 + 15级内部浮点 + 新人模式 + AI-assist规范 | Alice、Inko、Carol |
| W2 | 角色族adapter + 5个参考角色 + 假期感知归一化 | Carol、Bob |
| W3 | 经理cockpit `/v1_2/team` · coaching gap图 + 晋升包导出 + DID签字证据注入 | Bob、Henry |
| W4 | Trace v2 schema（HMAC + RoPA + 执行区域attest + degraded字段） + durable queue + DLQ + `router.reject`行 + `kms.decrypt`行 | Eve、Dan、Kris |
| W5 | 双锚：Sigstore Rekor + RFC 3161每日批 + 发射完整性计数 + workload绑定的Fulcio身份策略 | Henry、Dan、Kris |
| W6 | L4 review队列 + 人工传递性擦除 + sqlite-vec rebuild + `scim.rename`带版本DID alias链 | Eve、Alice、Carol |
| W7 | **HSM/KMS split-trust**（KMS DEK + Vault Transit HMAC分离角色）+ **TPM/Nitro-Enclave绑定的KMS grant** + legal hold L0闸门 + 吹哨人OWA + fs级read tripwire | Eve、Henry、Fiona、Kris |
| W8 | BambooHR SCIM 2.0入站 + WORM层（object-lock S3）+ DPIA pack + 合规矩阵（SOX 404/FINRA/MiFID II/PIPL/GDPR）+ 审计包SKU + Ollama三年TCO + 监管watch list预shred闸门 + 跨法域冲突仲裁UI | Carol、Henry、Fiona、Eve |

**测试目标**：v1.1基线533个 → **target +60 = 593个**。测试设计放在
`tests/v1_2/test-plan.md`。高价值fixture包括：公平性审计的统计正确性、
传递性擦除workflow边界、Sigstore锚失败回滚、HMAC key轮转验证器、
SCIM rename / re-hire / soft-delete边界。

**v1.3推迟项（90天后 · 2026-07至2026-09）**：

- 员工cockpit `/v1_2/me`完整UI
- HR cockpit只读 `/v1_2/calibration`
- 经理宽严尺overlay UI
- Workday SCIM入站
- ITSM/Jira结果桥 + counterfactual归因
- KMIP集成
- 三角色SoD enforcement matrix
- 监管watch list预shred闸门（多法域版本）

**v1.4推迟项（180天后 · 研究依赖）**：

- HR cockpit可编辑 + 受保护类别公平性审计仪表板
- CIO cockpit
- 跨embedding自动传递性擦除（+ threat model）
- counterfactual归因方法论paper
- 跨组织DID Skill VC验证（W3C VC 2.0 spec hardening闸门）

**v1.5+推迟项**：

- Personio SCIM
- QLDB集成
- 流式Rekor锚
- 多区租户federation

---

## 五 · 反立场（10条 · v4补到第10条）

1. **浏览器里不展示并排标量**。只展示叙事卡。CSV才有标量。
2. **不自动comp**。calibrated band流向HRIS，HR-of-record决策。
3. **不默认开闭源LLM**。Ollama优先；云opt-in并需per-purpose consent。
4. **没有"AI HR Manager"人设**。AI出证据，人决策。
5. **不用专有格式**。Markdown + frontmatter；**+ tenant-admin tar
   的DLP签字 + 吹哨人bypass**。
6. **合规不是"路线图上的事"**。DPIA / TIA / SCC / China Standard
   Contract / SOX 404 / FINRA 17a-4映射 v1.2 W8全交付。
7. **不做虚荣benchmark**。CI闸门强制source citation。
8. **crypto-shred不能"信任root用户"**。v1.2 W7 HSM/KMS强制。
9. **不把研究当ship出货**。embedding leak不可证 + counterfactual归因
   在v1.2明确为研究问题，配人工workflow + 采样审计替代方案。DPA
   披露残余风险。
10. **无single root of trust**（v4新增）：v1.2同时上split-trust HMAC +
    HSM绑定DID + TPM/Enclave绑定KMS。任何弱化都在DPA里披露残余风险。

---

## 六 · 商业模式（L7层 · v3新增）

### 6.1 审计包SKU rate card（`docs/commercial/PRICING.md`）

| Tier | anchor数/月 | 区域pin数 | 审计报告/年 | $/员工/月 | $/年（500员工）|
|---|---|---|---|---|---|
| Starter | 5万 | 1 | 1 | $4 | $24K |
| Pro | 50万 | 3 | 4 | $12 | $72K |
| Enterprise | 500万 | 无限 | 12 + 按需 | $28 | $168K |
| Regulated FS | 500万 + WORM + FINRA pack | 无限 | 12 + 按需 + 审计师支持 | $48 | $288K |

- **涨价闸门**：CPI + 3% / 年封顶。
- **超量**：$0.0002/anchor超tier上限。
- **解约通知**：行业标准12个月、我们18个月；与design partner谈到6个月。

### 6.2 TCO模型（`docs/operations/ollama-tco.md`）

per 100 / 500 / 1000 / 5000员工的三年对比TCO：

- **CapEx**：GPU SKU + 数量 + 安装 + 网络（4年直线折旧）。
- **OpEx**：电力（kW × $/kWh × PUE 1.4）+ 数据中心 + SRE FTE分配。
- **交叉点图**：$/百万token vs Anthropic/OpenAI/Bedrock。
- **风险调整**：平均40%利用率 + ±20%敏感性。
- **税务**：§179 / bonus depreciation。

### 6.3 cost-per-resolved-decision（v1.2指标；counterfactual放v1.3）

- **v1.2 reporting**：租户LLM cost ÷ resolved Decision数（内部artifact）。
- **防作弊采样审计**（Fiona）：随机5%的resolved case由独立reviewer
  复审（RBAC `audit:cost-validate`）。挂钩率MoM跳>20%触发警报。
- **v1.3新增**：外部系统桥（ITSM/Jira/SF closure）+ 文档化counterfactual
  归因方法论（因果weight、归因衰减、dedup window）。方法论paper +
  design partner签字**后**指标才进客户board slide。

### 6.4 合规=保险的EV模型（Fiona）

对§七里每个监管regime记录：

- **最大罚款exposure**：GDPR 2000万欧 / 收入4%；PIPL 5000万人民币 /
  收入5%；SEC 17a-4每违规5万美金；FINRA 17a-4(f)每违规1万美金 + 停业。
- **P(audit)每regime每年**（行业benchmark）。
- **P(finding | audit)** 用/不用Ome365对照。
- **EV避免罚款** = 最大罚款 × P(audit) × ΔP(finding)。
- 客户TCO模型把这一项列进去。

### 6.5 吹哨人 / 监管访问policy（`docs/policies/WHISTLEBLOWER.md`）

文档化bypass通道：Dodd-Frank §922 + EU Whistleblower Directive + UK PIDA。
完全out-of-band、绕开tenant-admin。签字导出包format。**租户无法
通过Ome365日志报复吹哨人**——因为吹哨人通道不出现在租户的DLP egress
日志里。

---

## 七 · 合规矩阵（v4 · 13个regime）

| 监管 | 条款 / 控制 | v1.2机制 | v1.3+ |
|---|---|---|---|
| GDPR | Art 5(1)(c) 数据最小化 | 默认只存HMAC | — |
| GDPR | Art 6法律依据 | 每行trace字段 | — |
| GDPR | Art 9特殊类别 | 分层consent + 逐类别 | — |
| GDPR | Art 17擦除权 | crypto-shred（HSM）+ 人工传递性 | 自动传递性 v1.4 |
| GDPR | Art 30 RoPA | trace字段 | — |
| GDPR | Art 32安全 | Sigstore + ed25519 + HSM | — |
| GDPR | Art 35 DPIA | 每backend每region模板 | — |
| GDPR | Art 44-49 传输 | 区域pin L4 + SCC模板 + provider attest区域 | — |
| PIPL | §13/§14分层consent | 逐purpose consent记录 | — |
| PIPL | §29敏感类别 | 逐类闸门 | — |
| PIPL | §38跨境 | 区域pin + China Standard Contract模板 + raw prompt结构阻断 | — |
| PIPL | §47删除权 | crypto-shred（HSM）| — |
| PIPL | §55 DPIA | 每backend每region模板 | — |
| EU AI Act 2027 | Art 9风险管理 + Art 14 人为oversight | 人审队列 + auditor RBAC + management assertion（v1.3）| management assertion v1.3 |
| SOX 404 | management assertion | — | v1.3（workflow + 季度cert）|
| SOX 404 | ITGC change管理 | Sigstore + RFC 3161 | — |
| SOX 404 | 控制失效exception log | — | v1.3 |
| SOX 404 | SoD dev/operator | RBAC matrix | enforcement v1.3 |
| SOC1 Type II | 外部审计师访问 | auditor RBAC | — |
| SOC2 | CC7.2监控 | Sigstore + 发射完整性 | — |
| ISO 27001 | A.12.4 logging | 同上 | — |
| FINRA | 17a-4(f) WORM | **object-lock S3 / MinIO retention**（v1.2 W8） | QLDB可选 v1.4 |
| SEC | 17a-4 | 同上WORM + 6年retention + 前2年立即可访问 | — |
| MiFID II | RTS 6 + RTS 22 | 同上WORM映射到投资决策trace | — |
| NYDFS | 23 NYCRR 500.15 | HSM/KMS FIPS 140-2 L2+ | — |
| Dodd-Frank | §922吹哨人 | OWA通道 + bypass + 外部公证人 | — |
| EU AI Act 2027 | 高风险系统注册 | role library + management assertion（v1.3）| v1.3 |
| US EEO-1 | 选填族裔 | opt-in公平性审计 | 完整dashboard v1.4 |

---

## 八 · 研究诚实（v3新增）

> 这两条**不是产品功能、是研究问题**。诚实命名比偷偷打包卖好。

### 8.1 通过派生embedding的不可泄漏证明

**问题**：当主体S的L1贡献被擦除，已经把S的措辞或实体共现pattern编码
进L3/L4的产物是**衍生作品**。用"去掉S"的数据集重建embedding会
产生**不同的**索引，但**不能证明**新索引对S的先前贡献零残留信号
（近重复cosine recall、n-gram透射、fine-tune corpus外部性）。

**v1.2交付**：人工workflow + tombstone + reviewer签字 + 从purged
数据源rebuild sqlite-vec。DPA披露残余风险。

**v1.4研究目标**：Ome365发表embedding leak threat model + reference
detection。在此之前，"自动传递性擦除"不进产品叙事。

### 8.2 cost-per-business-outcome的counterfactual归因

**问题**：把LLM成本除以resolved ticket数假设AI**导致**了resolve。
没有counterfactual（同一个人**没有**AI会不会也resolve？），指标就
**可以被opex膨胀地玩**。

**v1.2交付**：cost-per-resolved-decision（内部artifact、更简单的分母）
+ 防作弊采样审计 + 挂钩率挂钩警报。

**v1.3 design-partner工作**：方法论paper。三个候选approach：
(a) 倾向性score-matched ticket cohort pre/post AI；
(b) 租户内随机分配90天（consent-gated）；
(c) AI-eligibility cutoff附近的regression discontinuity。
**先发表方法论 + 客户签字模板，再让指标进客户board slide**。

### 8.3 embedding leak threat model（v1.4预告）

v1.2已知攻击面：

- rebuilt索引上的cosine近重复recall
- LLM fine-tune corpus外部性（若backend用prompt训练）
- 知识图谱实体共现persistence
- crypto-shred时序 vs backup snapshot retention
- 多租户邻近数据推理

---

## 九 · 待解决问题

**第5轮以前resolved**：

- ~~Q6 (Dan): 审计包SKU?~~ → §6.1 rate card
- ~~Q7 (Carol): v1.2有calibration room UI?~~ → v1.3只读 / v1.4可编辑
- ~~Q8 (Eve): HSM/KMS v1.2还是v1.3?~~ → v1.2 W7
- ~~Q9 (Bob): 人工证据注入的完整性?~~ → DID签字
- ~~Q10 (Alice): legal hold列表透明度?~~ → Rekor锚定 + 解除后通知主体 + 吹哨人bypass

**第6-7轮新增open question**：

- **Q11**（Jay/Fiona）：design-partner项目——v1.3指标ship前要签几家？
  建议 n ≥ 5。
- **Q12**（Kris）：embedding leak threat model timeline——谁做研究？
  in-house / 学术合作 / open call？
- **Q13**（Henry）：EU AI Act 2027映射是初步的，v1.2 ship前需要
  EU-qualified counsel复核。**预算？厂商？**
- **Q14**（Inko）：onboarding mode定义——"前180天"是租户可配置
  还是hard-coded？租户可配的话，谁定？是否有最低限（如90天强制）？
- **Q15**（Carol）：HRIS comp导出走哪个schema？OpenComp / ISO/IEC
  19944（HR Open）/ 厂商私有？

---

## 十 · 第7轮压力测试三战场总结（红队3 agent）

### 10.1 内部insider KMS妥协（红队 #1）

**场景**：senior insider有FastAPI shell + AWS KMS CMK admin（或Vault
Transit）。耐心。目标是**不可检测的forgery**。

**v1.2评级**：**catastrophic** 机密性 + 完整性；**severe** 不可否认；
**severe** 可用性。**v1.2有single root of trust，competent insider
能在不留痕迹的前提下拆掉它。**

**3条v1.2缺口**：

1. L0 keystore admin和HMAC种子保管**未隔离**。单KMS root同时解
   加密DEK和完整性HMAC key → 加密妥协 = 完整性妥协。
2. manager-DID ed25519**未HSM绑定**（v1.2 spec默认存为OS可读）。
   shell访问 = 签字能力。
3. Sigstore身份**未绑定tenant**（Fulcio ephemeral cert靠OIDC；
   无强制"只允许这个workload身份签T租户的批"策略）。

**v1.3硬化方案**（5条已进roadmap）：

1. **split-trust HMAC种子**：完整性key进Vault Transit的**不同**
   admin角色；2-of-3 quorum轮转。
2. **HSM绑定的manager DID**：ed25519进YubiHSM / CloudHSM不可导出；
   每次调用都需要attestation签字。
3. **Merkle Mountain Range + 外部发布**：每日root发到公链（Bitcoin
   OP_RETURN或CT log）——backdating外部就破。
4. **Witness cosigning**：每批由N个独立witness节点联签（跨租户
   attest网络）后才接受进Rekor。
5. **OIDC绑定的Sigstore身份策略**：verifier拒绝Fulcio subject与
   tenant-pinned workload身份不符的entry。

### 10.2 监管传票（法务红队 #2 · 6个子场景）

| # | 场景 | Ome365能证明 | 不能证明 | v1.3硬化 |
|---|---|---|---|---|
| 1 | SEC 17a-4传票，subpoena前GDPR Art 17已shred | Rekor锚定的shred + HMAC chain + WORM密文stub | subject X的明文 | **监管watch list预shred闸门**；SOX/SEC flagged租户的7年deferred shred队列 |
| 2 | EU AI Act 2027 Art 9 + Art 14 | 每prompt区域attest + 3角色签字 + Rekor锚 | Art 9生命周期（FRIA、残余风险接受）；Art 14**实质**oversight | **Art 14 oversight-friction**：必填理由字段 + reviewer credential绑定；**Art 9 FRIA模板**进onboarding |
| 3 | CAC PIPL §38/§55跨境 | 硬阻断telemetry + provider区域attest + Rekor路由 | provider sub-processor合规§55(3)；CAC SCC备案；§40 CIIO评估 | **provider sub-processor attestation chain**；**§55自评dossier自动生成**；CAC SCC workflow集成 |
| 4 | Dodd-Frank §922吹哨人报复 | 外部Rekor证据：legal hold + OWA提交时间戳 + 审计chain HMAC连续性 | **意图**（admin知道hallucination的事） | **Shamir分片的admin key**；retention policy修改需双控；OWA提交时即出**公证Rekor receipt** |
| 5 | PIPL §47 vs SEC 17a-4冲突（中国resident在美国受监管公司） | 路由进legal hold registry + 3角色冲突签字 | 哪个regime赢（法律问题） | **跨法域冲突仲裁UI**；statutory citation registry；自动escalate到外部counsel webhook。**SEC在broker-dealer受监管活动里赢**，PIPL §47(2)有carve-out |
| 6 | Sigstore Rekor history rewrite | 双锚：Rekor + RFC 3161 TSA（独立） | 哪个锚先被妥协 | **三锚**：+ Ethereum/Bitcoin OpenTimestamps L1 commitment；SIP-1 Witness Cosigning；NYDFS 23 NYCRR 500.16租户的离线air-gapped公证backup |

**跨场景v1.3硬化清单**：(a) 监管watch list预shred闸门；(b) Shamir分片
admin key；(c) 三锚；(d) FRIA + oversight friction；(e) 跨法域冲突
仲裁UI；(f) CAC §55 dossier自动生成。

### 10.3 运营失效（SRE红队 #3 · 3个子场景）

#### 10.3.1 vendor LLM outage

- **Anthropic API 4h区域outage，50个经理cockpit用户9am集体撞墙。**
- **保住的**：trace pipeline健康；router fast-fail emit
  `router.reject{purpose, backend, reason=upstream_429, ttl_ms}`行；
  queue drain；每日Merkle batcher**把reject行本身锚到Rekor + RFC 3161**。
  审计defensibility完整：**"我们试了，vendor挂了"是密码学可证的**。
  Sigstore批非空——reject row也是row。
- **v1.3缓解**：
  - LLM router自动fallback到本地Ollama，trace带`degraded=true` +
    `degraded_reason=upstream_outage`；UI banner"运行在本地模型上，
    质量降级"。
  - 每个purpose的circuit breaker：N次连续reject后flip purpose到
    `degraded`持续15分钟，返回cached/stale coaching分析并显式标注
    `staleness_ms`。
  - 每日Sigstore manifest新增`degraded_count` + `reject_count_by_backend`；
    admin UI显示24h滚动degraded ribbon。

#### 10.3.2 离职senior dev打包vault

- **场景**：senior dev在FastAPI host上有sudo，他离职前tar
  `/var/ome365/vault`传到个人Dropbox。
- **破的**：tar里有L0密文blob + sqlite-vec + markdown frontmatter
  （subject ID、时间戳、DID图）——**metadata泄漏严重**，即使明文未泄。
  有KMS instance role的情况下dev能在个人laptop上`kms:Decrypt`
  envelope DEK → 完整明文exfil。
- **DLP egress签字不catch这个**——它gate的是tenant-admin
  `/api/export/audit-pack`路由，**不是磁盘上的raw `tar`**。WORM S3
  不受影响（object lock防删不防读）。Sigstore锚证明事后没篡改，
  但说不出**谁读过**。
- **这是v1.2严重缺口**。**v1.3缓解**（建议升级为v1.2.1热修）：
  - **TPM/Nitro Enclave绑定的KMS grant**：DEK unwrap只能在attested
    enclave里发生；单独的instance role只能拿到密文blob。**离线
    tar = 废blob**。
  - **KMS CloudTrail → trace管线消费**：每条`Decrypt`变成`kms.decrypt`
    trace row带subject DID；异常detector flag每principal每小时
    >P99 unwrap量。
  - **fs级read tripwire**：`vault/`挂dm-verity + auditd `fanotify`；
    >1000个subject blob在<60秒内被bulk读触发admin alert + 自动
    撤销instance role。

#### 10.3.3 SCIM rename（BambooHR Carol Smith → Carol Jones）

- **场景**：Carol结婚改姓。SCIM 3am同步。Carol的vault entry keyed
  on `did:ome365:tenant1:carol-smith`。她周五的trace row用老DID。
  L4 wiki backlink用老名。Crypto-shred事件以"carol-smith"为key
  会不会影响"carol-jones"？
- **alias策略下保住的**：DID是immutable identifier；SCIM只改
  `displayName`/`userName`。周五锚定的trace仍有效——leaf hash未变。
  Crypto-shred targets DID而非display name。
- **6个子系统走查**：
  - vault：frontmatter `subject_did`不变；`display_name`更新；老名
    进`aliases[]`。
  - trace：零rewrite；resolver在render时把DID映射到当前name。
  - wiki：backlink靠DID resolve；老名字串搜过alias index命中。
  - 证据注入：cache key on DID，不受影响。
  - legal hold：scope on DID，rename不影响hold。
  - audit-pack：manifest emit `alias_history` block。
- **v1.3缓解**：
  - **canonical DID带版本** (`did:ome365:tenant1:u_8f3a@v2`)；SCIM写
    versioned alias，**永不**mutate ID。
  - **`name_resolver`服务**带60秒TTL，6个子系统的单一source；用
    lint规则禁止代码里用raw字符串name做key。
  - **`scim.rename`** trace行每日锚——rename变成可审计事件，不是
    无声mutation。

### 10.4 第7轮verdict + back-port决定

两个发现严重到必须**回退进v1.2**（不交付就不能ship）：

1. **§10.3.2 vault tar exfil** → v1.2 W7必含TPM/Nitro enclave绑定
   **或者**明确DPA披露"instance role妥协 = 完整明文exfil"。建议：
   交付绑定。timeline不允许时：交付DPA披露 + 30天v1.2.1热修。
2. **§10.1 KMS single root of trust** → v1.2 W7必须把HMAC种子从
   KMS CMK里隔出来（不同的Vault Transit角色）。2-of-3 quorum可
   等到v1.3，但种子隔离不行——这是**一天的工作**，关闭最严重
   的forgery path。

其余（witness cosigning、Bitcoin OTS、FRIA模板、Shamir分片、
监管watch list预shred、name_resolver service）进v1.3 roadmap §十二。

---

## 十一 · v4 ship候选（第8轮综合）

v4 = v3（第5轮）+ 第6轮差异化（§〇.2）+ 第7轮回退（§10.4）+ 下表10条。

| # | v3 | v4（本轮） | 来源 |
|---|---|---|---|
| 1 | KMS root同时解DEK和HMAC | **HMAC种子隔到独立Vault Transit角色**（一天工作）| R7 #1 |
| 2 | manager-DID ed25519（存储未定）| **manager-DID默认HSM绑定**（YubiHSM/CloudHSM）；fallback OS keyring需DPA披露 | R7 #1 |
| 3 | vault tar exfil未对应 | **W7上TPM/Nitro Enclave绑定的KMS grant**（或DPA披露 + v1.2.1热修路径）+ KMS CloudTrail → trace + fs read tripwire | R7 #3.2 |
| 4 | LLM outage produces "无审计数据"歧义 | **`router.reject`行 + 每日Sigstore manifest `degraded_count`** + 可选Ollama自动fallback | R7 #3.1 |
| 5 | SCIM rename歧义 | **带版本canonical DID** + `name_resolver`服务 + `scim.rename`锚定trace | R7 #3.3 |
| 6 | Sigstore身份=环境OIDC | **Workload绑定的Sigstore身份策略**：verifier拒绝非租户pin Fulcio | R7 #1 |
| 7 | 单锚（Rekor）+ 单TSA | **双锚W5确认**：Rekor + RFC 3161；**Bitcoin OTS三锚排进v1.3** | R7 #2.6 |
| 8 | 预shred可能撞subpoena | **监管watch list预shred闸门**：被覆盖类目shred事件需3角色复议 | R7 #2.1 |
| 9 | 买家叙事散落 | **§〇.2八条差异化 + §〇.4四问杀器移到首页可见** | R6 + Jay |
| 10 | "Workday vs我们"未addressed | §〇.1分类目楔子 + §〇.3并表表格 | R6 |

### v4买家定调（一段进pricing页）

> Workday和Lattice是HR的系统记录。Glean和Notion搜索并总结你的
> 数据。Mem0和Letta存agent memory。Vanta和Drata告诉你哪些系统
> 配置过了。**它们都不能回答：LLM在哪条prompt上看到了什么、谁签
> 的字、锚到哪个透明日志、当主体行使Art 17时每一个衍生数据发生
> 了什么？** 这才是Ome365记录的——以密码学方式、以Markdown明文、
> 可移植到你的文件系统、在Apache 2.0许可下。**我们是"先有治理后存
> memory"，而不是"先有memory后补治理"。**

### v4状态

- v1 → v2 21条 + v2 → v3 23条 + v3 → v4 10条 = **54条设计迭代**
- 4个专家轮 × 共17个sub-agent
- 2条研究诚实声明（§八）
- 4条买家杀器问（§〇.4）
- 1 ICP / 1 cockpit / 1 HRIS / 1 region / 8周（§四）

---

## 十二 · 8周路线图 + v1.3/v1.4/v1.5蓝图（第9轮）

### 12.1 W1-W8 周历（带exit criteria + 把关签字 + 阻塞依赖）

| W | 目标 | 退出标准（可测）| 签字角色 | 阻塞依赖 |
|---|---|---|---|---|
| W1 | 6维 + 5档 + 新人模式 + 15级内部浮点 + AI-assist规范 | (a)`/api/dimensions/score`返6×5 + 新人 ramp filter；(b) 8个dim测试通过；(c) AI_ASSIST_NORMS.md merged；(d) demo Alice/Inko在90天显示ramp band | Alice + Inko + Carol | 无 |
| W2 | 角色族adapter + 5个参考角色 + 假期感知归一化 | (a) `vault/Roles/*.md` schema validator；(b) 5角色ship（eng/design/PM/GTM/ops）；(c) 假期感知测试通过（产假/医假/sabbatical）| Carol + Bob | W1 |
| W3 | 经理cockpit `/v1_2/team` · coaching gap图 + 晋升包导出 + **HSM绑定DID签字证据注入** | (a) Vue cockpit加载15人合成团队；(b) coaching gap分层图渲染；(c) 晋升包导出ed25519签字 + manager-DID ≠ subject-DID enforced；(d) 自注入尝试 → 403 + 审计行 | Bob + Henry | W2 |
| W4 | Trace v2 schema（HMAC + RoPA + 执行区域attest + degraded字段）+ durable queue + DLQ + **`router.reject` + `kms.decrypt`行** | (a) trace schema validate 24字段；(b) RabbitMQ + DLQ + replay测试；(c) router.reject在模拟Anthropic 429下产trace；(d) kms.decrypt → trace管线集成 | Eve + Dan + Kris | W3 |
| W5 | **双锚**：Sigstore Rekor + RFC 3161每日批 + 发射完整性计数 + **workload绑定的Fulcio身份策略** | (a) 每日batcher产Merkle root；(b) Rekor entry可验 + TSA token可验；(c) 发射完整性对账 = LLM-counter；(d) verifier拒绝非租户pin Fulcio subject | Henry + Dan + Kris | W4 |
| W6 | L4 review队列 + 人工传递性擦除workflow + sqlite-vec rebuild + **`scim.rename`带版本DID alias chain** | (a) wiki:approve + wiki:propose RBAC tier；(b) erasure ticket队列 + reviewer签字 + rebuild；(c) SCIM rename测试过6子系统alias check | Eve + Alice + Carol | W3、W5 |
| W7 | **HSM/KMS split-trust**（KMS DEK + Vault Transit HMAC分离角色）+ **TPM/Nitro Enclave绑定KMS grant** + legal hold L0闸门 + 吹哨人OWA + **fs级read tripwire** | (a) HMAC种子in Vault角色A；KMS DEK admin in角色B；2-of-3 quorum轮转；(b) enclave attestation gating Decrypt；离线tar测试 = 密文only；(c) legal hold registry Rekor锚定；shred阻断subject测试；(d) OWA提交 → 外部公证人；不在tenant-admin DLP log里；(e) fanotify bulk read trigger | Eve + Henry + Fiona + Kris | W4、W5 |
| W8 | BambooHR SCIM 2.0入站 + WORM层（object-lock S3）+ DPIA pack + 合规矩阵 + 审计包SKU + Ollama三年TCO + **监管watch list预shred闸门** + **跨法域冲突仲裁** | (a) SCIM 2.0事件（create/update/soft-delete/rename/re-hire）都产trace；(b) object-lock retention 7年 + 6年/前2年immediate测试；(c) DPIA模板按backend × region填充；(d) PRICING.md + ollama-tco.md merged；(e) 监管watch list config + shred阻断测试；(f) PIPL §47 / SEC 17a-4冲突 → 3角色escalation UI | Carol + Henry + Fiona + Eve | W7 |

### 12.2 出货闸门（demo前必过）

- **G1**：533 → 593个测试绿（target +60）。覆盖：公平性审计、传递性
  擦除workflow、Sigstore锚失败回滚、HMAC轮转、SCIM rename边界、
  KMS split-trust、enclave attestation。
- **G2**：`scan_pii.py` 0 hit。CI闸门强制。
- **G3**：`truthguard lint` 0违规on compliance-matrix.md。
- **G4**：4位角色pre-demo签字：Bob（经理UX）、Eve（DPO签DPIA）、
  Henry（合规矩阵）、Dan（CIO采购）。
- **G5**：3位外部reviewer ack：(a) 审计师审trace schema；(b) EU资质
  counsel审EU AI Act映射（§九 Q13）；(c) 中国counsel审PIPL §38实现。

### 12.3 v1.3 roadmap（90天后 · 2026-07至09）

| 主题 | 交付 |
|---|---|
| **cockpit广度** | 员工cockpit `/v1_2/me`完整UI；HR cockpit只读 |
| **HRIS广度** | Workday SCIM 2.0入站（Personio CSV → SCIM放v1.4） |
| **结果桥** | cost-per-business-outcome带文档化counterfactual方法论 + design partner签字 |
| **AI Act成熟** | Art 14 oversight friction（理由字段 + reviewer credential绑定）；Art 9 FRIA模板；Art 72上市后监控 |
| **合规覆盖** | SOX 404 management assertion workflow + 季度cert；SoD enforcement matrix |
| **审计硬化** | 三锚（Bitcoin OpenTimestamps）；Witness Cosigning per SIP-1；3法域监管watch list预shred闸门运营 |
| **key保管** | KMIP集成；Shamir分片admin key（2-of-3 quorum）；NYDFS 500.16离线air-gapped公证tier |
| **GRC集成** | Vanta/Drata evidence导出adapter；OneTrust/Credo策略import |

### 12.4 v1.4 roadmap（180天后 · 2026-10至12 · 研究依赖）

| 主题 | 交付 |
|---|---|
| **embedding leak研究** | threat model paper + reference detection + L3/L4 embedding的自动传递性擦除 |
| **CIO cockpit** | `/v1_2/cio`带组织级FinOps + 控制覆盖 + 审计gap分析 |
| **HR cockpit可编辑** | calibration room带经理宽严尺overlay + 受保护类别公平性审计dashboard |
| **跨组织portability** | DID Skill VC验证（gated on W3C VC 2.0 spec hardening）|
| **connector广度** | Slack + Drive + Salesforce + Confluence + Jira connector（社区贡献、Apache 2.0）|
| **AI治理** | counterfactual归因方法论paper + 客户签字模板 |

### 12.5 v1.5+ roadmap（270天后 · 2027-01至03）

- Personio + Greenhouse + Rippling SCIM
- QLDB集成可选
- 流式Rekor锚（亚分钟级而非每日）
- 多区租户federation
- Sigstore Witness Cosign进我们的verifier
- EU AI Act 2027完整映射 + EU资质counsel attestation

---

## 十三 · 第10轮 · 最终ship状态

**v1.2 r5.0-final-zh 出货checklist**：

- [x] §〇.1 · 买家与楔子（中文最终版）
- [x] §〇.2 · 8条差异化（4个分析agent输出基础上中文重写）
- [x] §〇.3 · 与11款竞品并表（4个产品类目 × 11款命名产品）
- [x] §〇.4 · 4问杀器
- [x] §〇.5 · 8条诚实让步
- [x] §〇.6 · v3→v4的10条delta
- [x] §一 · 10角色画像（3买家 / 3用户 / 4把关人）
- [x] §二 · 7层v4架构（L0 split-trust / L1带版本 / L2 TPM绑定 / L3双锚）
- [x] §三 · 4大旗舰功能（in-scope vs deferred诚实分流）
- [x] §四 · 8周交付范围（1 ICP / 1 cockpit / 1 HRIS / 1 region）
- [x] §五 · 10条反立场（v4新增"无single root of trust"）
- [x] §六 · 商业模式L7（SKU + TCO + 防作弊 + EV保险 + 吹哨人policy）
- [x] §七 · 合规矩阵（13个regime映射）
- [x] §八 · 研究诚实（embedding leak + counterfactual归因）
- [x] §九 · 待解决问题（Q11-Q15 + 回退决定§10.4）
- [x] §十 · 第7轮压力测试3战场（KMS妥协 / 6监管 / 3运营）
- [x] §十一 · 第8轮v4综合
- [x] §十二 · 第9轮路线图（W1-W8 · v1.3 · v1.4 · v1.5）
- [x] §十三 · 第10轮 · 最终ship状态（本节）

### 10位角色最终签字（v4）

- **Alice**（资深IC）：yes —— HSM绑定DID + TPM绑定vault + 带版本DID封住3个缺口
- **Bob**（经理）：yes —— HSM绑定manager-DID让证据注入可enforce
- **Carol**（CHRO）：yes —— `scim.rename` + 监管watch list让HR操作可审计
- **Dan**（CIO）：yes —— split-trust + 双锚 + workload绑定Fulcio = 采购可辩护
- **Eve**（DPO）：yes —— TPM绑定 + read tripwire + 跨法域冲突UI = DPIA级
- **Fiona**（CFO）：yes —— 防作弊 + 三年TCO + EV保险不变；监管watch list降低spoliation EV
- **Henry**（合规）：yes —— 三锚roadmap + 6传票场景映射；WORM + 监管watch list兼容
- **Inko**（应届）：yes —— onboarding模式 + AI-assist gradient + 经理看不到单prompt不变
- **Jay**（CEO）：yes —— 8周scope保住；差异化进首页；楔子收紧
- **Kris**（客户CTO）：yes —— 诚实压力测试；single root of trust缺口关闭；研究诚实preserved

**最终quorum: 10 / 10**。**ship v1.2 r5.0-final-zh**。

---

## 附录A · 第6-7轮sub-agent来源（17位专家溯源）

- **第2轮**（5位 · v1批判）：Alice / Bob / Carol / Dan / Eve
- **第4轮**（5位 · v2批判）：Fiona / Henry / Inko / Jay / Kris
- **第6轮**（4位 · 跨产品分析）：HR-tech分析师（Workday/Lattice）、
  knowledge平台分析师（Glean/Notion）、AI-memory分析师（Mem0/Letta/
  Zep/Cognee）、GRC分析师（Vanta/Drata/OneTrust/Credo/Cranium）
- **第7轮**（3位 · 红队压力测试）：security红队（KMS妥协）、监管法务
  红队（6传票场景）、SRE红队（3运营失效场景）

完整critique log + benchmark log + stress-test log在ship时归档到
`docs/strategy/v1.2-round-{2,4,6,7}-*-log.md`。

---

## 附录B · 文档约束（中英文混排不加空格）

> 给Ome365中文contributor的写作规范，避免被lint reject：

- **中英文混排不加空格**："AI创新BU"而不是"AI 创新 BU"
- **数字和中文不加空格**："8周"而不是"8 周"
- **技术术语保英文**：HSM、KMS、Sigstore、Rekor、RFC 3161、DID、
  ed25519、HMAC、SCIM、TPM、Enclave、WORM等
- **法规条款保英文+中文释义**：`Art 17 擦除权`、`§38 跨境`、
  `17a-4(f) WORM`等
- **公司名/产品名保原文**：Workday、Lattice、Glean、Notion、Mem0、
  Letta、Zep、Cognee、Vanta、Drata、OneTrust、Credo、Cranium
- **不写无意义代码注释**：注释只写why-非obvious，不写what
